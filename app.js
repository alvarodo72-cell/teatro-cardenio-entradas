const $=id=>document.getElementById(id), sb=window.sb, {zones,palcoGroups}=window.CARDENIO;let currentView='patio',selected=new Map(),seats=[],seatByCoord=new Map(),statuses=new Map();
function validDni(d){return/^[0-9]{8}[A-Za-z]$/.test((d||'').trim())}function err(el,m){el.querySelector('.errorBox')?.remove();const d=document.createElement('div');d.className='errorBox';d.textContent=m;el.prepend(d)}function eventInfo(){const e=$('eventId');return e.value?{id:e.value,text:e.options[e.selectedIndex].text}:null}async function init(){await loadSeats();await loadEvents();bind();setView('patio')}async function loadSeats(){const {data,error}=await sb.from('seats').select('*');if(error)return alert('Error butacas: '+error.message);seats=data||[];seatByCoord=new Map(seats.map(s=>[s.coord,s]))}async function loadEvents(){const {data,error}=await sb.from('events').select('*').eq('estado','activo').order('fecha').order('hora');if(error)return alert('Error eventos: '+error.message);$('eventId').innerHTML=(data||[]).map(e=>`<option value="${e.id}">${e.nombre} · ${e.fecha} · ${String(e.hora).slice(0,5)}</option>`).join('');const has=(data||[]).length;$('noEvents').classList.toggle('hidden',has);document.querySelector('.zoneChooser').classList.toggle('hidden',!has);document.querySelector('.helpBar').classList.toggle('hidden',!has);$('mapArea').classList.toggle('hidden',!has);await loadStatuses()}async function loadStatuses(){statuses.clear();const f=eventInfo();if(!f){render();return}const {data}=await sb.from('event_seats').select('estado, seats(coord)').eq('event_id',f.id);(data||[]).forEach(r=>{if(r.seats?.coord)statuses.set(r.seats.coord,r.estado)});render()}function bind(){document.querySelectorAll('.zoneCard').forEach(b=>b.onclick=()=>setView(b.dataset.view));$('eventId').onchange=async()=>{selected.clear();await loadStatuses()};$('clear').onclick=()=>{selected.clear();render()};$('adminLoginBtn').onclick=()=>$('loginOverlay').classList.remove('hidden');$('closeLogin').onclick=()=>$('loginOverlay').classList.add('hidden');$('doLogin').onclick=login;$('checkout').onclick=openCheckout;$('closeCheckout').onclick=()=>$('checkoutOverlay').classList.add('hidden');$('backBuyer').onclick=()=>step('buyer');$('backAttendees').onclick=()=>step('att');$('closeTickets').onclick=()=>$('ticketOverlay').classList.add('hidden');$('printTickets').onclick=()=>window.print();$('goAttendees').onclick=goAttendees;$('goPayment').onclick=goPayment;$('finishOrder').onclick=createOrder}async function login(){const {data,error}=await sb.auth.signInWithPassword({email:$('adminEmail').value.trim(),password:$('adminPassword').value});if(error)return alert(error.message);const {data:p}=await sb.from('profiles').select('rol').eq('id',data.user.id).single();if(!p||!['superadmin','taquilla'].includes(p.rol))return alert('Usuario sin permisos');location.href='admin.html'}function setView(v){currentView=v;document.querySelectorAll('.zoneCard').forEach(b=>b.classList.toggle('active',b.dataset.view===v));$('viewTitle').textContent=v==='completo'?'Vista completa':zones[v].title;render()}function render(){const f=eventInfo();$('summaryEvent').textContent=f?f.text:'Sin función';const map=$('seatMap');map.innerHTML='';if(!f){updateSummary();return}map.innerHTML='<div class="stage">ESCENARIO</div><div class="zoneTitle">'+(currentView==='completo'?'Vista completa':zones[currentView].title)+'</div>';(currentView==='completo'?['patio','preferencia','palcos']:[currentView]).forEach(v=>{if(currentView==='completo')map.insertAdjacentHTML('beforeend','<div class="zoneTitle">'+zones[v].title+'</div>');v==='palcos'?renderPalcos(map):renderRows(map,v)});updateSummary()}function renderRows(map,v){for(let r=1;r<=zones[v].rows;r++){const row=document.createElement('div');row.className='row';zones[v].odds.forEach(n=>row.appendChild(btn(v,r,n)));row.appendChild(Object.assign(document.createElement('span'),{className:'aisle'}));row.appendChild(Object.assign(document.createElement('div'),{className:'rowLabel',textContent:r}));row.appendChild(Object.assign(document.createElement('span'),{className:'aisle'}));zones[v].evens.forEach(n=>row.appendChild(btn(v,r,n)));map.appendChild(row)}}function renderPalcos(map){const wrap=document.createElement('div');wrap.className='palcosExcel';palcoGroups.forEach(g=>{const col=document.createElement('div');col.className='palcoCol';col.innerHTML='<h3>'+g.title+'</h3>';g.nums.forEach((n,i)=>col.appendChild(btn('palcos',g.key,n,g.title,i+1)));wrap.appendChild(col)});map.appendChild(wrap)}function btn(zone,row,num,labelZone=null,fila=null){const b=document.createElement('button');b.className='seat '+(zone==='preferencia'?'preferencia':zone==='palcos'?'palco':'');b.textContent=num;const coord=zone==='palcos'?`palcos-${row}-${num}`:`${zone}-${row}-${num}`;const st=statuses.get(coord);if(st)b.classList.add(st);if(selected.has(coord))b.classList.add('selected');b.onclick=()=>{if(st&&st!=='libre')return;selected.has(coord)?selected.delete(coord):selected.set(coord,{coord,zone:labelZone||zones[zone].title,fila:fila||row,label:num,price:zones[zone].price});render()};return b}function updateSummary(){const vals=[...selected.values()];$('count').textContent=vals.length;$('total').textContent=vals.reduce((s,v)=>s+v.price,0)+' €';$('checkout').disabled=!eventInfo()||!vals.length;$('selectedList').innerHTML=vals.length?vals.map(v=>`<div><strong>${v.zone}</strong><br>${v.zone.startsWith('PALCOS')?'Plaza':'Fila'} ${v.fila} · Butaca ${v.label} · ${v.price} €</div>`).join(''):'Selecciona una o varias butacas libres.'}function step(s){$('buyerStep').classList.toggle('hidden',s!=='buyer');$('attendeesStep').classList.toggle('hidden',s!=='att');$('paymentStep').classList.toggle('hidden',s!=='pay');$('step1').classList.toggle('active',s==='buyer');$('step2').classList.toggle('active',s==='att');$('step3').classList.toggle('active',s==='pay')}function openCheckout(){const f=eventInfo();$('checkoutOverlay').classList.remove('hidden');$('checkoutEventName').textContent=f.text;step('buyer')}let buyer,attendees=[];function goAttendees(){buyer={nombre:$('buyerName').value.trim(),apellidos:$('buyerSurname').value.trim(),dni:$('buyerDni').value.trim().toUpperCase(),email:$('buyerEmail').value.trim(),telefono:$('buyerPhone').value.trim()};if(Object.values(buyer).some(x=>!x))return err($('buyerStep'),'Rellena todos los datos.');if(!validDni(buyer.dni))return err($('buyerStep'),'DNI incorrecto.');$('attendeeForms').innerHTML='';[...selected.values()].forEach((s,i)=>{const use=i===0&&$('buyerAttends').checked;$('attendeeForms').insertAdjacentHTML('beforeend',`<div class="attendeeCard"><h4>Entrada ${i+1}</h4><div class="seatMeta">${s.zone}<br>${s.fila} · Butaca ${s.label}</div><label>Nombre *<input class="an" value="${use?buyer.nombre:''}"></label><label>Apellidos *<input class="as" value="${use?buyer.apellidos:''}"></label><label>DNI *<input class="ad" maxlength="9" value="${use?buyer.dni:''}"></label></div>`)});step('att')}function goPayment(){attendees=[];const dnis=new Set(),cards=[...document.querySelectorAll('.attendeeCard')],seats=[...selected.values()];for(let i=0;i<cards.length;i++){const a={nombre:cards[i].querySelector('.an').value.trim(),apellidos:cards[i].querySelector('.as').value.trim(),dni:cards[i].querySelector('.ad').value.trim().toUpperCase(),seat:seats[i]};if(!a.nombre||!a.apellidos||!a.dni)return err($('attendeesStep'),'Faltan datos en entrada '+(i+1));if(!validDni(a.dni))return err($('attendeesStep'),'DNI incorrecto en entrada '+(i+1));if(dnis.has(a.dni))return err($('attendeesStep'),'DNI repetido: '+a.dni);dnis.add(a.dni);attendees.push(a)}const f=eventInfo(),tot=attendees.reduce((s,a)=>s+a.seat.price,0);$('paymentSummary').innerHTML=`<strong>Función:</strong> ${f.text}<br><strong>Comprador:</strong> ${buyer.nombre} ${buyer.apellidos}<br><strong>Total:</strong> ${tot} €<hr>`+attendees.map((a,i)=>`Entrada ${i+1}: ${a.nombre} ${a.apellidos} · ${a.dni} · ${a.seat.zone} ${a.seat.fila}/${a.seat.label}`).join('<br>');step('pay')}async function verify(f){const ids=attendees.map(a=>seatByCoord.get(a.seat.coord)?.id).filter(Boolean);if(ids.length!==attendees.length)return false;const {data}=await sb.from('event_seats').select('estado,seat_id').eq('event_id',f.id).in('seat_id',ids);if((data||[]).some(r=>r.estado&&r.estado!=='libre'))return false;const {data:t}=await sb.from('tickets').select('seat_id').eq('event_id',f.id).in('seat_id',ids).neq('estado','cancelada');return !(t||[]).length}async function createOrder(){const f=eventInfo();if(!await verify(f)){selected.clear();await loadStatuses();return alert('La butaca seleccionada ya no está disponible.\n\nEl plano se ha actualizado automáticamente. Por favor, selecciona otra butaca.')}const method=document.querySelector('input[name="payMethod"]:checked').value,estado=method==='bizum'?'pendiente_bizum':'pagado',now=Date.now(),num='CARN-2026-'+String(now).slice(-6),total=attendees.reduce((s,a)=>s+a.seat.price,0);const {data:order,error}=await sb.from('orders').insert({numero_pedido:num,event_id:f.id,comprador_nombre:buyer.nombre,comprador_apellidos:buyer.apellidos,comprador_dni:buyer.dni,comprador_email:buyer.email,comprador_telefono:buyer.telefono,metodo_pago:method,estado,total}).select().single();if(error)return alert('No se pudo crear el pedido.');const made=[];for(let i=0;i<attendees.length;i++){const a=attendees[i],seat=seatByCoord.get(a.seat.coord);const {data:t,error:te}=await sb.from('tickets').insert({numero_entrada:'ENT-'+String(now).slice(-6)+'-'+(i+1),order_id:order.id,event_id:f.id,seat_id:seat.id,nombre:a.nombre,apellidos:a.apellidos,dni:a.dni,estado:estado==='pagado'?'generada':'pendiente'}).select('*, seats(*)').single();if(te){selected.clear();await loadStatuses();return alert('La butaca seleccionada ya no está disponible.\n\nEl plano se ha actualizado automáticamente. Por favor, selecciona otra butaca.')}made.push(t);await sb.from('event_seats').upsert({event_id:f.id,seat_id:seat.id,estado:estado==='pagado'?'vendida':'pendiente_bizum',precio:a.seat.price},{onConflict:'event_id,seat_id'})}selected.clear();$('checkoutOverlay').classList.add('hidden');await loadStatuses();if(estado==='pagado'){renderTicketDocuments($('ticketsList'),made,f.text,order.numero_pedido);$('ticketOverlay').classList.remove('hidden')}else alert('Pedido pendiente de Bizum. La entrada se generará cuando el admin confirme el pago.')}init();
